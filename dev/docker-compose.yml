services:
  postgres:
    image: postgres:16-alpine
    container_name: helm-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: helm_local
    ports:
      - "5432:5432"
    volumes:
      - helm_pgdata:/var/lib/postgresql/data

volumes:
  helm_pgdata:
